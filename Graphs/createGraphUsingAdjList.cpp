#include <iostream>

using namespace std;

void insertEdge(int source, int dest, int* adj) {
    adj[u].push_back(v);
    adj[v].push_back(u);
}

void defineGraph(int nodes, int *adj) {
    vector<int> adj[n+1];
}
