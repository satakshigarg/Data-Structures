#include <iostream>

using namespace std;

void insertEdge(int source, int dest, int* adj, int val) {
    adj[source].push_back({dest, val)});
    adj[source].push_back({dest, val});
}

void defineGraph(int nodes, int *adj) {
    vector<pair<int, int>> adj[n+1];
}
